node_modules
.next
out
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
tsconfig.tsbuildinfo
coverage

# Supabase CLI local metadata (project ref, pooler URL, linked project, versions)
supabase/.temp/
supabase/.branches/

# Per-machine agent / IDE tooling (skills, MCP config, skill locks)
.agents/
.claude/
.cursor/
.kiro/
skills/
skills-lock.json
