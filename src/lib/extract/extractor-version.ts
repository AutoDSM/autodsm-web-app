/**
 * Bumped when extractor output shape or semantics change so clients can
 * show a re-scan affordance. Keep in sync with `BrandProfileMeta.extractorVersion`.
 */
export const EXTRACTOR_VERSION = 7;
